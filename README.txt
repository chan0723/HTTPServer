Features:

– Developed a Java based HTTP Server to support most features of HTTP/1.0 and HTTP/1.1 requests.– Used blocking queue and multi-threads to handle concurrent requests
– Extended server to dynamically load and unload multiple applications at different contexts via main menu.– Developed a set of servlets both to fetch static files and generate dynamic responses.